# Git-Branch-Name-Formatter
Just a very simple utility web page to get the formatted Git branch name.

For example, if the branch name is `Dashboard Bug Fixes @Username #Reference_ID`,  
the formatted branch name will be generated as `dashboard-bug-fixes-username-reference-id`.
